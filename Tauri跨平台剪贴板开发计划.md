# Tauri 跨平台剪贴板开发计划

版本：1.4
目标平台：Windows 10/11、macOS 13+  
产品形态：类似 Paste 的底部弹出式统一毛玻璃剪贴板；MVP 聚焦 Windows/macOS 本地剪贴板历史，账号与跨设备同步在后续版本提供

## 1. 计划基线

本计划按照以下团队配置估算：

- 1 名前端/桌面开发：React、TypeScript，逐步掌握必要的 Rust。
- 1 名后端/平台开发：Go、数据库、对象存储，同时负责 Windows/macOS 原生适配。
- UI 设计和测试由兼职人员支持。

预计周期：

| 阶段 | 预计时间 | 累计时间 | 面向对象 |
|---|---:|---:|---|
| P0 技术预研 | 2 周 | 2 周 | 开发团队 |
| MVP | 7 周 | 9 周 | 内部测试、种子用户 |
| 一期 | 10–12 周 | 19–21 周 | 账号与同步验证、公开发布 |
| 二期 | 10–14 周 | 29–35 周 | 增长、差异化、规模化 |

如果由一人独立开发，建议按 1.7–2.2 倍时间估算。以上时间不包含公司注册、隐私合规审核、应用商店审核等外部周期。

## 2. 技术架构

### 2.1 桌面客户端

| 层次 | 技术选择 |
|---|---|
| 桌面框架 | Tauri 2 |
| 界面 | React + TypeScript + Vite |
| 状态管理 | React Hooks；规模扩大后再评估独立状态库 |
| 长列表 | 游标分页；达到更大数据量后再引入虚拟列表 |
| 动画 | CSS Transform，必要时 Motion |
| 跨平台视觉 | 统一深色毛玻璃设计；共用布局、圆角、透明度、颜色和动效规范 |
| 桌面核心 | Rust + Tokio |
| 本地数据库 | SQLite + tokio-rusqlite + FTS5 |
| 本地文件 | 按 SHA-256 内容寻址的 Blob 缓存 |
| Windows 适配 | windows-rs + Win32/COM |
| macOS 适配 | objc2/AppKit，必要时少量 Swift/Objective-C |

Rust 使用范围应尽量收敛在系统层：MVP 负责剪贴板监听、窗口控制和本地存储；一期再加入加密和同步调度。展示、搜索交互、设置页面继续使用 TypeScript。

前端只维护一套毛玻璃组件和设计令牌，不为 Windows/macOS 分别设计界面。底层可分别调用 Windows DWM/窗口背景能力和 macOS `NSVisualEffectView` 实现相近的模糊、透明和着色效果，但这些差异不暴露为产品视觉分支。系统关闭透明效果、处于高对比度模式或原生效果不可用时，统一降级为不透明深色背景。

### 2.2 一期同步服务（MVP 不建设）

MVP 不依赖账号、网络或服务端，所有剪贴板数据只保存在本机。进入一期后，考虑到团队不熟悉 Rust，同步服务建议使用 Go：

| 模块 | 技术选择 |
|---|---|
| API 服务 | Go + net/http/chi |
| 实时通知 | WebSocket |
| 元数据 | PostgreSQL |
| 图片/文件 | S3 兼容对象存储 |
| 后台任务 | Go Worker；一期初期不引入复杂消息队列 |
| 身份认证 | 邮箱验证码或 OAuth，短期 Access Token + Refresh Token |
| 加密 | 客户端 AES-256-GCM 或 XChaCha20-Poly1305 |

WebSocket 只发送“同步游标变化”通知，真实数据通过 HTTPS 增量拉取，便于重连、限流和故障恢复。

### 2.3 核心数据模型

```text
ClipboardItem
├── id: ULID
├── type: text | image | files | rich_text
├── representations[]
├── preview
├── blob_hashes[]
├── content_hash
├── source_app
├── group_id: ULID | null
├── grouped_at
├── created_at
├── pinned
└── deleted_at

ClipboardGroup
├── id: ULID
├── name
├── sort_order
├── created_at
└── updated_at
```

一个项目允许保存多种表示，例如纯文本和 HTML。MVP 使用内容哈希和“程序写入抑制窗口”避免应用自身回写形成重复记录。`group_id` 非空的项目属于用户分组，自动清理任务必须跳过，只允许用户主动移出分组或删除。一期加入同步时，再通过数据库迁移增加 `origin_device_id`、`origin_item_id`、`sync_state`、`expires_at` 等字段，并与内容哈希共同防止设备间循环同步。

## 3. P0：技术预研阶段

周期：2 周。

这一阶段不追求完整界面，只验证最容易导致项目失败的系统能力。

### 3.1 工作内容

- 建立 Tauri 2 + React 工程，配置 Windows/macOS 构建流水线。
- Windows 使用 `WM_CLIPBOARDUPDATE` 捕获文本、图片和 `CF_HDROP` 文件列表。
- macOS 使用 `NSPasteboard.changeCount` 捕获文本、图片和文件 URL。
- 注册全局快捷键，显示无边框、置顶、底部定位窗口。
- 验证统一毛玻璃窗口在 Windows/macOS 上的模糊、透明度、文字对比度和降级效果。
- 验证多显示器、不同 DPI、macOS Spaces、Stage Manager 和全屏应用。
- 记录原活动窗口，验证“写入剪贴板”和“自动粘贴回原应用”。
- 验证 macOS 辅助功能授权流程以及 Windows `SendInput` 的权限限制。
- 验证 SQLite 写入、缩略图生成和 1 万条记录查询性能。

### 3.2 退出标准

- Windows/macOS 均能稳定捕获文本、图片和普通文件。
- 快捷键到窗口首帧的热启动 P95 不超过 250ms。
- 窗口能显示在当前活动显示器底部，不覆盖任务栏/Dock。
- Windows/macOS 使用同一布局和视觉令牌，毛玻璃不可用时均能自动降级且保持可读。
- 连续复制 1,000 次无崩溃、无死循环、无明显内容丢失。
- 明确列出无法保真的格式，例如 Office 私有格式和虚拟文件对象。

如果这些标准无法达到，应在开始 MVP 功能开发前调整窗口或原生适配方案。

## 4. MVP：核心闭环

周期：P0 后 7 周。目标是向 20–50 名种子用户验证 Windows/macOS 上“本地剪贴板历史查找 + 常用内容分组 + 快速回写”的核心价值。

### 4.1 MVP 功能范围

#### 剪贴板采集

- 支持纯文本、PNG/JPEG 图片、单个或多个普通文件。
- 保存来源应用、时间、类型、字符数/文件大小等基本信息。
- 基于 SHA-256 对相同内容全局去重；再次复制或粘贴已有内容时移动原记录到顶部。
- 默认保存最近 500 条或 30 天，可配置其中一个简单上限。
- 文件在本机先保存路径引用；路径失效时明确提示。

#### Paste 风格面板

- 全局快捷键唤出和隐藏。
- 底部搜索列表 + 右侧内容预览布局。
- Windows/macOS 使用同一套中性深色毛玻璃界面，不提供平台专属皮肤或材质分支。
- 键盘上下导航、回车选择、Esc 关闭。
- 列表显示文本摘要、图片/文件图标、来源应用和复制时间，预览区显示完整内容或缩略图。
- 按关键词搜索文本和文件名。
- 固定、删除、清空历史。
- 托盘菜单：打开、暂停记录、退出。

#### 常用内容分组

- 面板提供“最近”入口和用户自定义分组，可在不离开主面板的情况下新建分组。
- 支持分组重命名和删除；删除分组默认将其中内容移回普通历史，不直接删除内容。
- 支持通过“移入分组”菜单将当前项目加入指定分组，并可将项目移出分组。
- 点击分组后只展示该分组内容；搜索作用于当前分组，“最近”中搜索全部本地历史。
- 分组中的项目不受“最近 500 条或 30 天”自动清理规则影响，只有用户主动删除才会移除。
- “清空历史”默认只清理普通历史，不删除分组及分组内项目；清空分组内容需要单独确认。
- 分组创建、切换、移入和移出操作均支持键盘访问，鼠标拖放可作为增强交互而非唯一操作方式。

#### 回写和粘贴

- 点击或回车将内容写回系统剪贴板。
- 选择内容后自动切回原应用并粘贴。macOS 必须先取得辅助功能权限；Windows 遇到管理员目标或焦点/输入限制时保留剪贴板内容并提示手动按 `Ctrl+V`。
- 写回自身剪贴板时不重复产生历史记录。

#### 本地隐私与设置

- 一键暂停或恢复剪贴板记录。
- macOS 提供基于 Bundle ID 的应用排除列表；Windows 不提供用户应用排除功能，但遵守系统剪贴板的敏感与历史排除标记。
- 剪贴板历史、缩略图和文件引用仅保存在本机，不上传网络。
- 提供历史保留上限和一键清空本地数据。
- 清理任务必须保留所有分组内项目，并在设置页说明分组内容的永久保留规则。
- 日志不得记录剪贴板正文、文件名或其他敏感信息。

#### 双平台交付

- 同一套 MVP 功能同时支持 Windows 10/11 与 macOS 13+。
- 同一套 React 页面、组件结构和设计令牌覆盖两个平台，只在 Tauri/Rust 窗口层做必要的原生适配。
- 提供可安装、可卸载的 Windows 安装包和 macOS DMG。
- 完成 macOS Apple Silicon 与 Windows 10/11 x64 常见 DPI 的基础回归测试。

### 4.2 MVP 非目标

- 不承诺 Word、Excel、Adobe 等私有格式完全还原。
- 不提供登录、注册、账号体系和设备管理。
- 不提供云端存储、跨设备同步和跨设备文件传输。
- 不支持超大文件和云盘占位文件的特殊处理。
- 不做 OCR、AI 分类、团队共享和浏览器扩展。
- 不做嵌套分组、复杂标签、智能规则和移动端。
- 0.1.0 内测包暂不签名或公证；正式发布前再加入 Developer ID、Windows 代码签名和公证流程。

### 4.3 MVP 验收指标

| 指标 | 目标 |
|---|---|
| 快捷键到面板首帧 | 热启动 P95 ≤ 250ms |
| 空闲 CPU | 24 小时平均 < 1% |
| 本地采集可靠性 | 连续复制 1,000 次无明显内容丢失 |
| 本地搜索性能 | 1 万条历史下查询 P95 ≤ 100ms |
| 崩溃率 | 100 次核心流程测试无崩溃 |
| 回写去重 | 10,000 次写回测试不产生重复记录循环 |
| 分组可靠性 | 重启应用、清理普通历史后，分组和分组内项目保持完整 |
| 分组操作 | 新建、重命名、删除、移入、移出及当前分组搜索均通过测试 |
| 视觉一致性 | Windows/macOS 核心页面布局、间距、颜色和交互一致；透明效果关闭时仍清晰可用 |
| 平台覆盖 | Windows 10/11 x64、macOS 13+ Apple Silicon 核心流程通过 |
| 本地隐私 | 无账号、无云端依赖，日志不包含剪贴板正文 |

### 4.4 MVP 周计划

| 周次 | 主要工作 |
|---|---|
| 第 1–2 周 | 平台采集器、SQLite、Blob 缓存、缩略图 |
| 第 3–4 周 | 底部面板、快捷键、搜索、键盘交互、托盘 |
| 第 5 周 | 分组数据模型、新建/管理分组、移入/移出、分组搜索和永久保留规则 |
| 第 6 周 | 回写剪贴板、自动粘贴、去重、本地隐私设置和异常处理 |
| 第 7 周 | Windows/macOS 回归测试、性能优化、安装包、签名和种子用户发布 |

## 5. 一期：可公开发布版本

周期：MVP 后 10–12 周。目标是在本地核心体验稳定后，引入账号和加密跨设备同步，并达到可长期使用、可公开下载的产品质量。

### 5.1 账号与基础跨设备同步

- 邮箱验证码或 OAuth 登录，并提供设备列表、设备重命名和退出登录。
- 首先支持两台设备同步文本和压缩后的图片。
- 云端只保存客户端密文，服务端不持有可解密正文的密钥。
- 新设备通过一次性配对码或 QR 配对获得账户主密钥。
- WebSocket 发送同步游标变化通知，客户端通过 HTTPS 增量拉取真实数据。
- 离线产生的数据进入本地队列，恢复网络后自动补传。
- 文件正文功能完成前，只同步文件卡片及其“当前设备不可用”状态。
- 提供同步开关、同步状态和失败重试入口；退出登录不删除本地历史。

### 5.2 文件跨设备同步

- 上传真实文件内容，而不是同步原路径。
- 文件加密后进入对象存储。
- 目标设备按需下载到本地缓存，再写入系统剪贴板。
- 单文件默认上限 100MB，总缓存空间可配置。
- 支持多文件、上传进度、失败重试和取消。
- SHA-256 内容去重，服务端引用计数和垃圾回收。

### 5.3 同步与安全完善

- 设备远程撤销和异常登录提醒。
- 恢复密钥及明确的密钥丢失提示。
- 每设备独立密钥，撤销设备后无法读取新数据。
- 完整的 Tombstone 删除同步和存储清理。
- 同步状态可视化：等待、上传、下载、失败、已完成。
- API 限流、异常登录提醒和设备审计记录。
- 30/90/永久等保留策略。

### 5.4 内容能力

- HTML/RTF 富文本作为多表示保存和回写。
- URL、颜色值、代码片段等本地类型识别。
- 在 MVP 分组基础上增加排序、颜色、图标及导入/导出能力。
- 搜索范围扩展到来源应用、标签和文件名。
- 本地生成缩略图，缩略图同样加密同步。

### 5.5 产品化

- 完整设置中心、首次启动和权限引导。
- Windows/macOS 安装包签名，macOS 公证。
- Tauri Updater，更新包和更新清单签名。
- 开机启动、单实例、睡眠/唤醒和网络切换处理。
- 崩溃收集和性能指标；正文及敏感信息必须脱敏。
- 隐私政策、服务条款、账号删除和数据导出。
- 自动化构建、灰度发布、版本回滚。

### 5.6 一期发布门槛

- Windows 10/11 x64、macOS Apple Silicon 完成回归测试；Intel 支持在一期另行验证。
- 7×24 小时常驻稳定性测试通过。
- 1 万条历史下搜索和横向滚动无明显卡顿。
- 网络抖动、睡眠唤醒、Token 过期后能自动恢复。
- 服务端单实例故障不导致客户端本地历史不可用。
- 完成威胁建模、密钥恢复演练和账号删除验证。
- Beta 用户核心会话无崩溃率达到 99.5% 以上。

## 6. 二期：差异化与规模化

周期：一期后 10–14 周。只根据一期用户数据选择投入，不建议一次全部开发。

### 6.1 推荐优先级 A

- 局域网设备直传：同一网络优先 P2P，云端负责发现和回退。
- 大文件断点续传、分片上传和下载续传。
- 本地 OCR：搜索截图文字，结果只在本地或加密后同步。
- 更强的格式保真：Office 常用 HTML/RTF、透明 PNG、文件夹。
- 智能规则：按来源应用、类型、大小决定是否记录或同步。
- 敏感内容识别：验证码、密码、银行卡号仅本地处理并默认短期保存。

### 6.2 推荐优先级 B

- 多工作区和安全共享空间。
- 企业版设备策略、管理员控制和审计。
- 自托管同步服务。
- 浏览器扩展和 Web 管理端。
- iOS/Android 伴侣应用；受系统剪贴板限制，不承诺后台持续监听。
- Passkey 登录和硬件安全密钥。

### 6.3 二期规模化工作

- 后端无状态化、水平扩容和跨区域对象存储。
- 数据库按用户或时间分区，完善游标同步压测。
- Blob 生命周期管理、孤儿对象扫描和成本告警。
- 服务端 SLO、告警、备份、恢复演练和灾难恢复。
- 订阅计费、容量配额和滥用控制。

## 7. 测试策略

### 7.1 自动化测试

- MVP Rust 单元测试：格式解析、哈希去重、本地持久化、分组保留规则和回写抑制。
- TypeScript 测试：搜索、选择、分组管理、键盘导航和状态管理。
- 一期 Rust/Go 测试：加密、同步状态机、游标同步、权限校验和对象引用计数。
- 一期契约测试：客户端与服务端协议兼容性。
- 数据迁移测试：每次版本升级验证 SQLite Schema。

### 7.2 系统测试矩阵

- Windows 10、Windows 11；100%、125%、150%、200% DPI。
- macOS 13+ Apple Silicon；单屏、多屏、全屏、Spaces、Stage Manager；Intel 纳入一期扩展矩阵。
- 来源应用：浏览器、Office、微信/企业微信、Finder/Explorer、代码编辑器、截图工具。
- 数据：长文本、大图、多文件、同内容重复复制、失效路径和特殊字符文件名。
- 一期增加网络矩阵：离线、慢网、丢包、切换 Wi-Fi、代理、休眠后恢复。

## 8. 主要风险与应对

| 风险 | 影响 | 应对措施 |
|---|---|---|
| macOS 缺少剪贴板变化事件 | 空闲功耗、漏采集 | 自适应轮询 `changeCount`，唤醒和前台变化时主动检查 |
| 文件跨设备不是路径同步 | 文件不可用 | 必须上传真实内容，目标端下载后再构造本地文件列表 |
| 自动粘贴权限和焦点不稳定 | 用户误以为失败 | macOS 将辅助功能设为强制门槛；Windows 写入成功但受 UIPI/焦点限制时明确提示手动 `Ctrl+V` |
| 原生毛玻璃效果跨平台差异 | 两端观感不一致或文字难读 | 统一设计令牌并限制透明度范围；原生效果只作为背景层，不可用时降级为不透明深色背景 |
| 多格式剪贴板差异 | 格式丢失 | 采用 `representations[]`，保留原始格式与通用格式 |
| 分组项目被自动清理或误删 | 常用内容丢失 | 清理 SQL 强制排除 `group_id` 非空项目；删除分组默认移回历史并二次确认 |
| 同步产生无限循环 | 流量和数据爆炸 | 来源 ID、内容哈希、抑制窗口、幂等 API 四层防护 |
| 云端泄露剪贴板 | 严重隐私事故 | 从首次云同步开始端到端加密，服务端不处理明文 |
| 团队缺少 Rust 经验 | 延期和质量风险 | Rust 仅放系统核心；服务端用 Go；P0 提前验证 FFI 和窗口能力 |
| 签名、公证、更新失败 | 无法发布 | MVP 发布前完成签名和测试渠道，不在发布日期首次尝试 |

## 9. 人员分工建议

### 桌面/前端开发

- React UI、虚拟列表、搜索和设置。
- Tauri Command 接口和前端权限配置。
- 本地数据库查询、缩略图展示和性能优化。
- 安装包、自动更新和桌面端测试。

### 平台/后端开发

- Windows/macOS 剪贴板和窗口适配。
- MVP 阶段协助 SQLite、Blob 缓存、安装包和双平台稳定性。
- 一期负责 Go 同步服务、PostgreSQL、对象存储和 WebSocket。
- 一期负责加密、设备配对、文件上传下载和状态机。
- CI、监控、备份和安全工作。

### 兼职设计/测试

- 底部面板、卡片、空状态和权限引导。
- 系统测试矩阵、回归清单和发布验收。
- 种子用户反馈整理和优先级评审。

## 10. 关键决策

1. 第一版支持 Windows 10/11 x64 和 macOS 13+ Apple Silicon，暂不支持 Linux、Windows ARM64 或 macOS Intel。
2. Tauri 负责窗口、托盘、权限和发布；复杂剪贴板逻辑采用自定义 Rust 平台模块；Windows/macOS 共用一套中性毛玻璃界面，不维护 Liquid Glass 等平台专属视觉版本。
3. MVP 完全本地运行，不要求登录，不建设同步服务，先验证基础剪贴板体验。
4. MVP 支持一级自定义分组；分组内容永久保留，普通历史清理不得影响分组数据。
5. 一期服务端使用 Go，降低团队同时学习 Rust 服务端生态的成本。
6. 始终本地优先：进入一期后，即使服务器不可用，历史、搜索和回写仍可工作。
7. 从第一次云同步开始端到端加密；一期先同步文本和图片，再实现文件正文同步。
8. 二期功能必须依据一期留存率、文件使用率和用户反馈选择，避免同时开发 AI、团队和移动端。

## 11. 阶段成果定义

| 阶段 | 可交付成果 |
|---|---|
| P0 | 双平台技术原型、风险报告、性能基线 |
| MVP | Windows/macOS 可安装的本地剪贴板内部测试版，包含采集、历史、搜索、分组永久保留、固定、删除和回写 |
| 一期 | 可公开发布的账号与加密同步版本，包含文本/图片同步、真实文件同步、设备安全和自动更新 |
| 二期 | 局域网直传、大文件、OCR/规则或企业能力中的高价值组合 |
